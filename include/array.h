#include <stdbool.h>
#include "dp_array.h"

// =======================================================================

void array(const bool utility, const int Vmax, const struct objects_t * objects);
